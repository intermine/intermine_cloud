// eslint-disable-next-line max-len
import { Model200TemplateResponseAllOfItems } from '@intermine/compose-rest-client'

/**
 * There are other properties as well, but here
 * we are only add those properties which we want.
 */
export type TTemplateResponseData = {
    items: Model200TemplateResponseAllOfItems
}
