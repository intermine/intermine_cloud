export enum Entities {
    Dataset,
    Template,
    Mine,
}

export enum UploadFileStatus {
    Successful,
    Fail,
    FailToUploadFile,
}

export enum FileTypes {
    Fasta,
    Gff,
    Tsv,
    Csv,
    Isa,
    Zip,
    Tar,
    Unknown,
}
