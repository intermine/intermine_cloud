apiVersion: v1
kind: Service
metadata:
  name: {{ include "intermineinstance.fullname" . }}
  labels:
    {{- include "intermineinstance.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "intermineinstance.selectorLabels" . | nindent 4 }}
