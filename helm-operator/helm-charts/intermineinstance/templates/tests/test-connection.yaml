apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "intermineinstance.fullname" . }}-test-connection"
  labels:
    {{- include "intermineinstance.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "intermineinstance.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
